import { Mail } from "lucide-react";
import { ContactForm } from "./contact-form"; import { LandingFooter } from "./landing-footer"; import { LandingNavbar } from "./landing-navbar"; import { getDictionary, type Locale } from "@/lib/i18n";
export function ContactPage({ locale }: { locale: Locale }) { const dictionary = getDictionary(locale); const copy = dictionary.landing.contactPage; return <main className="landing-palette min-h-screen bg-[var(--landing-bg)] text-[var(--landing-text)]"><LandingNavbar locale={locale} /><section className="relative border-b border-[color:var(--landing-border)] bg-[var(--landing-hero-bg)]"><div className="landing-dot-grid absolute inset-0" /><div className="relative mx-auto max-w-7xl px-4 py-20 sm:px-6 lg:px-8"><p className="text-xs font-black uppercase tracking-[.24em] text-[var(--landing-accent-strong)]">{copy.eyebrow}</p><h1 className="mt-4 max-w-4xl text-5xl font-black uppercase leading-none sm:text-7xl">{copy.title}</h1><p className="mt-6 max-w-2xl text-lg text-[var(--landing-copy)]">{copy.description}</p></div></section><section className="mx-auto grid max-w-7xl gap-8 px-4 py-16 sm:px-6 lg:grid-cols-[.8fr_1.2fr] lg:px-8"><div><p className="border border-[color:var(--landing-border)] bg-[var(--landing-panel-muted)] p-4 text-sm text-[var(--landing-copy)]">{copy.responseNote}</p><div className="mt-4 grid gap-3">{copy.channels.map((channel) => <article key={channel.title} className="border border-[color:var(--landing-border)] bg-[var(--landing-panel)] p-5"><Mail className="h-5 w-5 text-[var(--landing-accent-strong)]" /><h2 className="mt-3 text-lg font-black">{channel.title}</h2><p className="mt-2 text-sm text-[var(--landing-copy)]">{channel.description}</p><a className="mt-3 block font-bold text-[var(--landing-accent-strong)]" href={`mailto:${channel.value}`}>{channel.value}</a></article>)}</div></div><ContactForm copy={copy.form} /></section><LandingFooter locale={locale} /></main>; }
